import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function ProductUploadAdminPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Product Upload</CardTitle>
      </CardHeader>
      <CardContent>Upload new products via form or CSV.</CardContent>
    </Card>
  )
}
