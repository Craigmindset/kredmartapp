import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function TrackOrdersAdminPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Track Orders</CardTitle>
      </CardHeader>
      <CardContent>Track shipping and delivery status of orders.</CardContent>
    </Card>
  )
}
