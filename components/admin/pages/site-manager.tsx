import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function SiteManagerAdminPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Site Manager</CardTitle>
      </CardHeader>
      <CardContent>Configure global site settings, banners, and content.</CardContent>
    </Card>
  )
}
